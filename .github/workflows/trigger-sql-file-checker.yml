name: Call Schema Change Monitor

on:
  push:
    branches:
      - main

jobs:
  trigger_schema_change-monitor:
    uses: halit-dincer-mark43/test-sql-action/.github/workflows/sql-file-check.yml@main
    secrets:
      slack_webhook_url: ${{ secrets.SLACK_WEBHOOK_URL }}
